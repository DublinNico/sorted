// Stub for static assets (CSS, images) imported in source files.
// Jest cannot process these file types — this mock returns an empty
// value so the import resolves without error.
module.exports = "";
